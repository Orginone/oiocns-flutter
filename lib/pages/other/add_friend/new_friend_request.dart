import 'package:flutter/widgets.dart';

class NewFriendRequest extends StatelessWidget {
  const NewFriendRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
